# Admin login password (shared)
ADMIN_PASSWORD=

# Backend API base URL (no trailing slash)
BACKEND_URL=http://localhost:3001

# Must match backend ADMIN_SECRET — used when proxying requests to backend
ADMIN_SECRET=
